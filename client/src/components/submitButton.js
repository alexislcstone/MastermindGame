import './submitButton.css'

export default function SubmitButton(){
  //need to work on the onSubmit logic here
    //check if any numbers are in original sequence
    //check if the corect numbers' locations are correct
    //make a post request to guesses table
  return(
    <div>
      <input type='button' className='submit-button' value='Submit Button'/>
    </div>
  )
}